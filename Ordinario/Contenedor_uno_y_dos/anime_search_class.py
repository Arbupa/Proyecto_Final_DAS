class Anime_Search():
	def __init__(self, id, title, episodes, image, synopsis, tipo, rated, score, airing, members ) -> None:
		self.id = id
		self.title = title
		self.episodes = episodes
		self.image = image
		self.synopsis = synopsis
		self.tipo = tipo
		self.rated = rated
		self.score = score
		self.airing = airing
		self.members = members