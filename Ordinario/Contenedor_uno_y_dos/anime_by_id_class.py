class Anime_By_Id():
	def __init__(self, id, title, episodes, status, score, rank, duration, synopsis, premiered, broadcast) -> None:
		self.id = id
		self.title = title
		self.episodes = episodes
		self.status = status
		self.score = score
		self.rank = rank
		self.duration = duration
		self.synopsis = synopsis
		self.premiered = premiered
		self.broadcast = broadcast